{
  "name": "@compiler/source",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "all": "nx run-many -t lint format test build",
    "build": "nx run-many -t build && pnpm --filter @compiler/compiler run copy-types",
    "lint": "nx run-many -t lint format",
    "test": "nx run-many -t test"
  },
  "private": true,
  "devDependencies": {
    "@monodon/rust": "2.3.0",
    "@napi-rs/cli": "3.0.0-alpha.63",
    "@napi-rs/wasm-runtime": "^0.2.4",
    "@nx/workspace": "21.6.4",
    "emnapi": "^1.1.0",
    "nx": "21.6.4"
  }
}
