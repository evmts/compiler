{
  "name": "@compiler/source",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "all": "bun build:zig && bun test:zig && bun lint:zig && nx run-many -t lint test build",
    "lint:zig": "zig fmt --check .",
    "test:zig": "zig build test",
    "build:zig": "zig build && zig build wasm",
    "build:ts": "node scripts/generate-ts-bindings.js"
  },
  "private": true,
  "devDependencies": {
    "@monodon/rust": "2.3.0",
    "@napi-rs/cli": "3.0.0-alpha.63",
    "@napi-rs/wasm-runtime": "^0.2.4",
    "@nx/workspace": "21.6.4",
    "emnapi": "^1.1.0",
    "nx": "21.6.4"
  },
  "dependencies": {
    "solc": "^0.8.30"
  }
}
